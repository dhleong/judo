// Top-level build file where you can add configuration options common to all sub-projects/modules.

allprojects {
    buildscript {
        repositories {
            mavenCentral()
        }
    }
    repositories {
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

ext.vs = [
    kotlin: '1.1.3',

    commonsNet: '3.6',
    guava: '22.0',
    jline: '3.3.0',
    jython: '2.7.1b3',
    moshi: '1.5.0',
]

ext.deps = [
    kotlin: "org.jetbrains.kotlin:kotlin-stdlib-jre8:${vs.kotlin}",

    commonsNet: "commons-net:commons-net:${vs.commonsNet}",

    guava: "com.google.guava:guava:${vs.guava}",

    jline: "org.jline:jline:${vs.jline}",
    jython: "org.python:jython-standalone:${vs.jython}",

    moshi: "com.squareup.moshi:moshi:${vs.moshi}",

    test: [
        assertj: 'org.assertj:assertj-core:2.4.1',
        junit: 'junit:junit:4.12',
    ],
]

ext.config = [
    name: 'Judo',
    version: '1.0.2',
]
